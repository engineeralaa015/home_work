//5. Declare an integer and a double variable representing
//your age and your height in meters
//respectively. Then declare a num variable and assign it
// the sum of the two variables. Print all three.

void main() {
  int age = 32;
  double height = 169.5;
  print(height);
  print(age);
  num result;
  result = (height + age);
  print(result);
}
